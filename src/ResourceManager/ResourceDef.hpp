#pragma once

#include <cstdint>

namespace Lumen {
namespace ResourceManager {

enum class TextureID {
        TEST_TEXTURE,
        NUMBER_OF_TEXTURES,
};

enum class SpriteID {
        TEST_SPRITE,
        NUMBER_OF_SPRITES,
};

enum class FontID {
        DROID_FONT,
        NUMBER_OF_FONT,
};


} // namespace ResourceManager
} // namespace Lumen
