#pragma once

#include <cstdint>

namespace Lumen {
namespace ResourceManager {

enum class TextureID {
        TEST_TEXTURE,
        UI_TEXTURE,
        MENU_SENCE_BACKGROUND,
        NUMBER_OF_TEXTURES,
};

enum class SpriteID {
        TEST_SPRITE,
        MENU_SCENCE_BACKGROUND_SPRITE,
        MENU_BUTTON_SPRITE,
        NUMBER_OF_SPRITES,
};

enum class FontID {
        DROID_FONT,
        NUMBER_OF_FONT,
};


} // namespace ResourceManager
} // namespace Lumen
